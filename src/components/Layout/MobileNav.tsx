import React, { ReactElement } from 'react';
import { NavItemProps } from '.';
import cx from 'clsx';

export interface Props {
  navItems: NavItemProps[];
}

export default function MobileNav(props: Props): ReactElement {
  return <div>Mobile nav TODO</div>;
}
