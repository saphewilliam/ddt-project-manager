import React, { ReactElement } from 'react';
import { NavItemProps } from '.';
import cx from 'clsx';

export default function MobileNavItem(props: NavItemProps): ReactElement {
  return <div>Mobile nav item TODO</div>;
}
