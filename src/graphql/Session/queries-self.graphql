query getSession($token: String!) {
  session(token: $token) {
    id
    expiresAt
    team {
      id
      name
      acronym
    }
    user {
      id
      displayName
      firstName
      lastName
    }
    member {
      id
      role
    }
  }
}
