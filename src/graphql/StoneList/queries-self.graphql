fragment StoneListStone on Stone {
  name
  alias
  alias2
  hex
  hex2
  order
  stoneTypeId
}

query StoneList($userId: String!, $stoneId: String!) {
  stoneList(userId: $userId, stoneId: $stoneId) {
    id
    amount
  }
}

query UserInventory($userSlug: String!) {
  userStoneList(userSlug: $userSlug) {
    id
    amount
    stone {
      id
      ...StoneListStone
    }
  }
  userAttributeList(userSlug: $userSlug) {
    id
    amount
    attribute {
      id
      namePlural
    }
  }
  user(userSlug: $userSlug) {
    id
    firstName
    lastName
  }
  stoneTypes {
    id
    name
  }
}

query Inventory {
  attributes {
    id
    name
    namePlural
    attributeLists {
      id
      amount
      userId
    }
  }
  stones {
    id
    ...StoneListStone
    stoneLists {
      id
      amount
      userId
    }
  }
  stoneTypes {
    id
    name
  }
  stoneListUsers {
    id
    displayName
  }
}
