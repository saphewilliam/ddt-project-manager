mutation updateStoneList(
  $stoneId: String!
  $userId: String!
  $amount: Int!
) {
  updateStoneList(
    stoneId: $stoneId
    userId: $userId
    amount: $amount
  ) {
    id
    user {
      id
      firstName
      lastName
    }
    stone {
      id
      name
    }
    amount
  }
}