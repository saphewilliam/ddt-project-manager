mutation UpdateStoneInventory($stoneId: String!, $userId: String!, $amount: Int!) {
  updateStoneInventory(stoneId: $stoneId, userId: $userId, amount: $amount) {
    id
    user {
      id
      firstName
      lastName
    }
    stone {
      id
      name
    }
    amount
  }
}
